// whenever a function is called it gets a layer in the stack memory (this layer is called stack frame ),all the variables and parameters of that particular functions also gets ocuupied in the stack frame 
// whenever the functions returns the stack frame gets deleted 
// current function is in top of the stack