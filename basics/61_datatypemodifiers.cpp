/*
--since int - 4 bytes - 32 bits - where each bit can be either zero or one 
--hence its range -  (-2^31 to 2^31-1)     its actually comes out to be 2^32  but int can have positive as well as negative integers so we divide it by 2 

*/

// datatype modifiers are used alter the meaning of the existing datatype 
// 1. long - 
// 2. long long(instead of long long int we can also write long long ) -
// 3. short -
// 4. signed - both positive and negative 
// 5. unsigned - only positive numbers  
